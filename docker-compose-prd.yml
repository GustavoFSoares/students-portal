version: '3.7'

services:

  avag:
    container_name: avag
    build:
      context: .
      dockerfile: ./docker/Dockerfile-prd
    ports:
      - '80:3000'
    volumes:
      - ./:/usr/share/nginx/html:cached
